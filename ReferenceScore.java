public class ReferenceScore {
    public String reference_players; //Creates an array group for reference_players
  	public double reference_scores; //Creates an array group for reference_scores

public ReferenceScore(String player, double score) {
    reference_players = player;
    reference_scores = score;
  }
}
